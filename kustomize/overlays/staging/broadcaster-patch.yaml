apiVersion: apps/v1
kind: Deployment
metadata:
  name: broadcaster
  namespace: project
spec:
  template:
    spec:
      containers:
        - name: broadcaster
          env:
            - name: WEBHOOK_URL
              $patch: delete
