apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-log-claim
  namespace: project
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
